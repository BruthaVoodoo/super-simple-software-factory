[project]
name = "sssf-development"
version = "0.0.0"
requires-python = ">=3.11"
dependencies = []

[dependency-groups]
test = ["pydantic>=2,<3", "pyyaml>=6,<7", "python-dotenv>=1,<2", "rich>=13,<15"]

[tool.uv]
package = false
